{
  "_comment": "Live SEP-24 GET /info deposit capture (#1095), captured by hand against the anchor's own advertised transfer server the same way tests/fixtures/sep38/capability-capture.json was. SEP-6 /info (https://api.anclap.com/transfer6/info) was checked too and shows the identical ARS/PEN-only shape.",
  "capturedAt": "2026-08-30T00:45:00.000Z",
  "anchorId": "anclap",
  "homeDomain": "anclap.com",
  "registeredAssetCode": "USDC",
  "transferServerSep0024": "https://api.anclap.com/transfer24",
  "reachable": true,
  "supportsDeposit": false,
  "reason": "The deposit map covers ARS and PEN — the anchor's own fiat-pegged tokens for the registered usdc-ars/usdc-pen corridors — but has no USDC entry. Same shape as cowrie: withdraw is advertised for the registered asset, deposit is not.",
  "info": {
    "deposit": {
      "ARS": {
        "enabled": true,
        "authentication_required": true,
        "min_amount": 11,
        "max_amount": 500000,
        "fee_fixed": 10,
        "fee_percent": 1
      },
      "PEN": {
        "enabled": true,
        "authentication_required": true,
        "min_amount": 2,
        "max_amount": 3000,
        "fee_fixed": 1,
        "fee_percent": 1
      }
    }
  }
}
